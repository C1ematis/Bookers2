        <tr>
          <th>コメント</th>
          <th colspan= "1" class= "col-md-9"></th>
          <th colspan= "1" class= "col-md-1"></th>
        </tr>
        <% book.book_comments.each do |comment| %>
        <tr>
          <td><%= link_to user_path(comment.user.id) do %>
            <%= attachment_image_tag comment.user, :profile_image, size:"30x30", format: 'jpeg', fallback:"no_image.jpg" %>
            <br><%= comment.user.name %>
          <% end %></td>
          <td>
            <%= comment.comment %>
          </td>
          <td>
            <% if comment.user.id == current_user.id %>
              <%= link_to "Destroy", book_book_comment_path(comment.book, comment), method: :delete, remote: true, class: "btn btn-danger btn-sm" %>
            <% end %>
          </td>
        </tr>
      <% end %>
